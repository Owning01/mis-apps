# Fix report antislop 001 - 2026-09-23

Aprobado por el usuario: todos (1-9). Cambios solo en `index.html` + nuevo `DESIGN.md`.

## Fixes
1. R-02: em dash eliminado de `<title>` (`index.html:6`), `document.title` y fallback `fmtDate`. Grep confirma 0 apariciones en `index.html` y `DESIGN.md`.
2. R-27: estados dedicados. Carga con esqueletos `role="status"` en las 3 grillas (mismo ancho de card, sin mover layout). Error con `role="alert"`: nombra los repos fallidos y boton Reintentar (44px, con foco visible). Probado con `?repos=noexiste/nada`: aparece la caja con el repo y el boton.
3. R-01: proposito del halo escrito en comentario CSS (foco de marca sobre el header, 9%).
4. R-04: relevancia de iconos escrita en comentario JS (mapeo por seccion).
5. R-06: razon tipografica escrita en comentario CSS (display geometrica + cuerpo legible).
6. R-14: uniformidad de cards justificada en comentario CSS (catalogo de igual peso; ruptura de ritmo solo en Paginas).
7. R-29: avatares de 6 pares a 3 (menta, ambar, naranja), dentro del sistema verde/naranja.
8. R-31: razones de una linea en comentarios del codigo + seccion Decisiones en `DESIGN.md`.
9. Dials: `DESIGN.md` con Dial ENERGY 1 / RHYTHM 2 / MOTION 1, Design Read y motivo.

## Evidencia (Playwright, local 8010)
- Titulo con guion, 0 em dash en el DOM, 4 cards, esqueletos reemplazados. 0 errores de consola.
- Click-through: tabs Apps/Temp/Paginas OK (0/14/4), modal abre (3 versiones) y cierra con X, busqueda "cine" da 1 resultado y limpiar vuelve a 4.
- Error simulado: caja con `noexiste/nada` + Reintentar + `role="alert"`.
- Movil 375: sin scroll-X, 1 columna, tabs deslizables.
- Capturas: `preview-antislop-desktop.png`, `preview-antislop-mobile.png`.

## Delivery Gate
- Bloque 1 Hard Gate: 17 no. Bloque 2 Purpose-Gate: 12 no (cada tecnica con proposito escrito). Bloque 3 Liveliness: 7 si (dials, foco, blancos, acento naranja unico, motivo avatar+chips, Design Read). Bloque 4 Craft: 14 no. PASS.
